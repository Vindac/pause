# Changelog

本项目的所有重要变更都记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.0.0] - 2026-08-27

跨平台重写：整仓由 Swift/SwiftUI（仅 macOS）迁移至 **Tauri 2（Rust 后端 + Svelte 5 / TypeScript 前端）**，同时支持 **macOS 与 Windows**。功能与 v1.0.0 保持 1:1 对齐，原有 30 个单元测试全部以等价用例移植。

### 新增

- Windows 平台完整支持：托盘常驻、无边框提醒窗、键鼠空闲检测（GetLastInputInfo）、锁屏会话通知（WTS）与电源广播唤醒监听、多显示器工作区定位
- GitHub Actions 双平台构建流水线（macOS dmg + Windows msi/nsis）
- 内置合成提示音 chime.wav（替代 NSSound "Tink"）
- 演示模式与诊断日志保留：`PAUSE_DEMO=1`、`PAUSE_DEMO_SETTINGS=1`、`PAUSE_DEBUG=1`

### 变更

- 设置：「自动开始休息」开关与「自动倒计时」选择器合并为一个下拉，新增「**无倒计时**」选项（选中即关闭自动开始，回到纯手动模式）
- 修复：设置窗修改任意设置项（含自动倒计时秒数）会导致整个应用卡死——`set_setting` 在持锁语句块内对同一把设置互斥锁二次加锁，同线程自死锁并冻结主线程；已改为「加锁改值 → 释放锁 → 落盘 → 副作用」的统一写入路径
- 修复：中文界面下「延迟休息时间」快捷选项误显示英文单位 `min`，现随语言显示「X 分钟」
- 修复：提醒/休息页不随语言设置显示中文——提醒窗为独立 webview，此前未初始化文案字典导致 `t()` 回退显示键名
- 修复：设置窗点关闭后无法再次打开——Tauri 默认关闭即销毁窗口，现拦截关闭事件改为隐藏（macOS 惯例）
- 变更：休息时长由步进器改为下拉选择（1–10 分钟、30、60；历史自定义值显示为「自定义」项）
- 应用图标沿用原版「窗户+远山+太阳」意象，经 `tauri icon` 生成全平台尺寸
- 「覆盖其他窗口」在两平台统一以 always-on-top 实现（近似原 .screenSaver 层级）
- 提醒窗口淡入淡出与透明度过渡改由前端 CSS 驱动（时长不变：0.35s 入 / 0.5s 出 / 0.2s 调整），保证双平台观感一致

### 架构

- Rust 层承载全部业务逻辑与系统能力（状态机/设置/壁纸管线/平台桥/托盘），每秒心跳在主线程串行推进——对齐原版 `@MainActor` 单线程模型；前端为纯视图层
- 文案表内建于 Rust `l10n.rs`（54 条中英双语），语言切换整体推送前端并重建原生菜单，双端永不漂移
- 已知平台差异详见 README「已知差异」小节（Windows 托盘文字、提示音资源、cartoon 兜底插画简化）

## [1.0.0] - 2026-08-26

首个正式版本。

### 新增

- macOS 菜单栏常驻：实时显示距下次休息的剩余时间，不占据 Dock
- 提醒间隔 30/45/60 分钟快捷选项 + 自定义（10–180 分钟）
- 按真实使用时间计时：离开电脑 / 显示器睡眠 / 系统睡眠期间自动暂停，累计真实使用满间隔才提醒（可关闭）
- 自动开始休息：提醒弹出后「开始休息」按钮附带逐秒倒计时（10/20/30/60 秒可选），期间无操作自动进入休息（可关闭）
- 延迟休息 1–5 分钟快捷选项 + 自定义（1–15 分钟），次数不限
- 自定义休息时长（1–30 分钟）
- 全幅网络壁纸提醒窗口（900×600 自适应缩放、16pt 圆角、Ken Burns 缓动、交叉淡化）
- 壁纸管线：后台预取 + 本地缓存（最多 18 张自动淘汰、≤3200px 降采样）+ 无网络时运行时生成兜底图
- 手动切换壁纸与预览
- 休息倒计时页 + 提前结束
- 暂停 / 继续提醒、立即休息
- 开机自动启动（SMAppService）
- 提醒窗口透明度调节（30%–100%，实时生效）、覆盖其他窗口选项
- 中 / 英文界面切换（默认英文），即时生效
- 多显示器支持：提醒在鼠标所在屏幕居中弹出
- 锁屏 / 屏幕保护 / 屏幕睡眠期间到期提醒自动暂缓，恢复后弹出
- 提醒时轻提示音
- 30 个单元测试（状态机 / 空闲顺延 / 自动休息 / 缓存淘汰 / 布局缩放 / URL 校验）

[1.0.0]: https://github.com/Vindac/pause/releases/tag/v1.0.0
[2.0.0]: https://github.com/Vindac/pause/releases/tag/v2.0.0
